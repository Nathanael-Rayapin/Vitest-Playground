{
  "scripts": {
    "test": "vitest --project default",
    "test-verbose": "vitest --reporter verbose",
    "test-extend": "vitest --project extend",
    "test-vi-utility": "vitest --project vi-utility",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.17",
    "vitest": "^4.0.17"
  }
}
